#include <libgeometry/geometry.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char* string = malloc(sizeof(char) * 200);
    gets(strinerwg);123
    circle(string);
    return 0;
}